---

---

<section id="acerca" class="about">
	<div class="container">
		<h2 class="section-title animate-on-scroll">Acerca de Mí</h2>
		<div class="about-content">
			<div class="about-text">
				<h3 class="name">Luis Miguel Guere Balvín</h3>
				<p class="bio">
					Ingeniero de Software especializado en Inteligencia
					Artificial con base en Huancayo y Pucallpa, Perú. Mi
					formación multidisciplinaria abarca desde el desarrollo de
					software y machine learning hasta la mecatrónica y ciencias
					ambientales. Me enfoco en crear soluciones tecnológicas
					innovadoras con IA que generen un impacto positivo en el
					entorno y la sociedad peruana.
				</p>
				<div class="education">
					<h4>Formación Académica</h4>
					<div
						class="education-item senati-certificate"
						onclick="openCertificateModal()"
					>
						<span class="institution">SENATI</span>
						<span class="degree"
							>Ingeniería de Software con Inteligencia Artificial</span
						>
						<span class="certificate-indicator"
							>📜 Ver Certificado</span
						>
					</div>
					<div class="education-item">
						<span class="institution"
							>Universidad Continental - Huancayo, Perú</span
						>
						<span class="degree">Ingeniería Mecatrónica</span>
					</div>
					<div class="education-item">
						<span class="institution"
							>Universidad Nacional de Ucayali</span
						>
						<span class="degree">Ciencias Ambientales</span>
					</div>
				</div>
				<div class="mission">
					<h4>Misión</h4>
					<p>
						Desarrollar tecnología que impacte positivamente en el
						entorno, explorando la intersección entre distintas
						disciplinas para crear soluciones innovadoras que
						combinen IA, software y mecatrónica en proyectos de
						ingeniería ambiental.
					</p>
				</div>
			</div>
		</div>
	</div>
</section>

<!-- Certificate Modal -->
<div id="certificateModal" class="certificate-modal">
	<div class="modal-content">
		<div class="modal-header">
			<h3 class="modal-title">Certificado SENATI</h3>
			<button class="close-btn" onclick="closeCertificateModal()"
				>&times;</button
			>
		</div>
		<div class="pdf-container">
			<img
				src="/senati.png"
				alt="Certificado SENATI"
				class="certificate-image"
			/>
		</div>
	</div>
</div>

<style>
	.about {
		padding: 6rem 2rem;
		background: var(--bg-color);
		position: relative;
		overflow: hidden;
	}

	.container {
		max-width: 1000px;
		margin: 0 auto;
		position: relative;
		z-index: 1;
	}

	.section-title {
		font-family: var(--font-main);
		font-size: 3rem;
		font-weight: 700;
		text-align: center;
		margin-bottom: 4rem;
		color: var(--text-primary);
		letter-spacing: -0.02em;
	}

	.about-content {
		background: var(--glass-bg);
		backdrop-filter: blur(20px);
		border: 1px solid var(--glass-border);
		border-radius: 0; /* Sharp corners */
		padding: 3rem;
		box-shadow: 0 4px 24px rgba(0, 0, 0, 0.05);
		transition: all 0.4s ease;
	}

	.about-content:hover {
		transform: translateY(-2px);
		box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
		border-color: var(--text-primary);
	}

	.name {
		font-family: var(--font-main);
		font-size: 2rem;
		font-weight: 600;
		margin-bottom: 1.5rem;
		color: var(--text-primary);
		text-align: center;
		letter-spacing: -0.01em;
	}

	.bio {
		font-size: 1.1rem;
		line-height: 1.7;
		color: var(--text-secondary);
		margin-bottom: 3rem;
		text-align: center;
		max-width: 800px;
		margin-left: auto;
		margin-right: auto;
	}

	.education,
	.mission {
		margin-bottom: 3rem;
	}

	.education h4,
	.mission h4 {
		font-family: var(--font-main);
		font-size: 1.3rem;
		font-weight: 600;
		color: var(--text-primary);
		margin-bottom: 1.5rem;
		text-align: left;
		border-bottom: 1px solid var(--glass-border);
		padding-bottom: 0.5rem;
	}

	.education-item {
		display: flex;
		flex-direction: column;
		margin-bottom: 1rem;
		padding: 1.5rem;
		background: rgba(0, 0, 0, 0.02);
		border-radius: 0; /* Sharp corners */
		border-left: 2px solid var(--text-primary);
		transition: background 0.2s ease;
	}

	@media (prefers-color-scheme: dark) {
		.education-item {
			background: rgba(255, 255, 255, 0.05);
			border-left-color: var(--text-primary);
		}
	}

	.education-item:hover {
		background: rgba(0, 0, 0, 0.05);
	}
	@media (prefers-color-scheme: dark) {
		.education-item:hover {
			background: rgba(255, 255, 255, 0.08);
		}
	}

	.institution {
		font-weight: 600;
		color: var(--text-primary);
		margin-bottom: 0.3rem;
		font-size: 1.1rem;
	}

	.degree {
		color: var(--text-secondary);
		font-size: 0.95rem;
	}

	.senati-certificate {
		cursor: pointer;
	}

	.certificate-indicator {
		font-size: 0.8rem;
		color: var(--text-primary);
		opacity: 0.8;
		margin-top: 0.5rem;
		display: block;
		font-weight: 500;
	}

	.mission p {
		color: var(--text-secondary);
		line-height: 1.7;
		text-align: left;
		font-style: normal;
	}

	/* Modal Styles */
	.certificate-modal {
		display: none;
		position: fixed;
		z-index: 1000;
		left: 0;
		top: 0;
		width: 100%;
		height: 100%;
		background-color: rgba(0, 0, 0, 0.5);
		backdrop-filter: blur(5px);
	}

	.modal-content {
		position: relative;
		margin: 3% auto;
		width: fit-content;
		max-width: 90%;
		max-height: 85%;
		background: var(--bg-color);
		border: 1px solid var(--glass-border);
		border-radius: 0; /* Sharp corners */
		box-shadow: 0 20px 50px rgba(0, 0, 0, 0.2);
		display: flex;
		flex-direction: column;
	}

	.modal-header {
		padding: 1rem 2rem;
		background: var(--bg-color);
		border-bottom: 1px solid var(--glass-border);
		display: flex;
		justify-content: space-between;
		align-items: center;
	}

	.modal-title {
		color: var(--text-primary);
		font-family: var(--font-main);
		font-size: 1.2rem;
		font-weight: 600;
		margin: 0;
	}

	.close-btn {
		background: none;
		border: none;
		color: var(--text-primary);
		font-size: 2rem;
		cursor: pointer;
		padding: 0;
		width: 40px;
		height: 40px;
		display: flex;
		align-items: center;
		justify-content: center;
		border-radius: 50%;
		transition: all 0.2s ease;
	}

	.close-btn:hover {
		background: rgba(0, 0, 0, 0.05);
	}
	@media (prefers-color-scheme: dark) {
		.close-btn:hover {
			background: rgba(255, 255, 255, 0.1);
		}
	}

	.pdf-container {
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 2rem;
		min-height: 400px;
		background: var(--bg-color);
	}

	.certificate-image {
		max-width: 100%;
		max-height: 70vh;
		width: auto;
		height: auto;
		object-fit: contain;
		border-radius: 0;
		box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
	}

	@media (max-width: 768px) {
		.about {
			padding: 4rem 1rem;
		}

		.section-title {
			font-size: 2.2rem;
			margin-bottom: 3rem;
		}

		.about-content {
			padding: 2rem;
		}

		.name {
			font-size: 1.5rem;
		}

		.education-item {
			text-align: left;
		}

		.modal-content {
			width: 95%;
			max-width: 95%;
			margin: 5% auto;
		}
	}
</style>
