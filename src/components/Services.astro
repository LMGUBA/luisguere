---
import AndroidIcon from "./AndroidIcon.astro";
---

<section id="servicios" class="services">
	<div class="container">
		<h2 class="section-title animate-on-scroll">Servicios</h2>
		<div class="services-grid">
			<div class="service-card glass">
				<div class="service-icon">
					<div class="icon-circle">ML</div>
				</div>
				<h3>Machine Learning</h3>
				<p>
					Desarrollo de algoritmos inteligentes en Python para
					análisis predictivo y automatización de procesos.
				</p>
			</div>
			<div class="service-card glass">
				<div class="service-icon">
					<div class="icon-circle">WEB</div>
				</div>
				<h3>Desarrollo Web Flask y Astro</h3>
				<p>
					Aplicaciones web modernas y rápidas utilizando Flask para
					backend y Astro para frontend estático.
				</p>
			</div>
			<div class="service-card glass">
				<div class="service-icon">
					<div class="icon-circle">AND</div>
				</div>
				<h3>Desarrollo de Aplicaciones Android</h3>
				<p>
					Aplicaciones móviles nativas para Android utilizando Kotlin
					con arquitecturas modernas y mejores prácticas.
				</p>
			</div>
			<div class="service-card glass">
				<div class="service-icon">
					<div class="icon-circle">IoT</div>
				</div>
				<h3>IoT y Proyectos Ambientales</h3>
				<p>
					Soluciones de Internet de las Cosas para monitoreo
					ambiental, automatización de dispositivos y sostenibilidad.
				</p>
			</div>
			<div class="service-card glass">
				<div class="service-icon">
					<div class="icon-circle">MEC</div>
				</div>
				<h3>Mecatrónica</h3>
				<p>
					Integración de sistemas mecánicos, electrónicos y de control
					para automatización.
				</p>
			</div>
			<div class="service-card glass">
				<div class="service-icon">
					<div class="icon-circle">ENV</div>
				</div>
				<h3>Ciencias Ambientales</h3>
				<p>
					Consultoría y desarrollo de soluciones tecnológicas para
					sostenibilidad ambiental.
				</p>
			</div>
		</div>
	</div>
</section>

<style>
	.services {
		padding: 6rem 2rem;
		background: var(--bg-color);
		position: relative;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
	}

	.section-title {
		font-family: var(--font-main);
		font-size: 3rem;
		font-weight: 700;
		text-align: center;
		margin-bottom: 4rem;
		color: var(--text-primary);
		letter-spacing: -0.02em;
	}

	.services-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
	}

	.service-card {
		/* Glass effect inherited from global .glass */
		padding: 2.5rem;
		text-align: left;
		transition: all 0.3s ease;
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		background: var(--glass-bg); /* Ensure background is set */
	}

	.service-card:hover {
		transform: translateY(-5px);
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
		border-color: var(--text-primary);
	}

	.service-icon {
		margin-bottom: 1.5rem;
		color: var(--text-primary);
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.icon-circle {
		width: 64px;
		height: 64px;
		border-radius: 50%;
		border: 2px solid var(--text-primary);
		display: flex;
		align-items: center;
		justify-content: center;
		font-family: var(--font-main);
		font-size: 0.75rem;
		font-weight: 600;
		letter-spacing: 0.05em;
		color: var(--text-primary);
		transition: all 0.3s ease;
	}

	.service-card:hover .icon-circle {
		background: var(--text-primary);
		color: var(--bg-color);
		transform: scale(1.05);
	}

	.service-card h3 {
		font-family: var(--font-main);
		font-size: 1.5rem;
		font-weight: 600;
		margin-bottom: 1rem;
		color: var(--text-primary);
		letter-spacing: -0.01em;
	}

	.service-card p {
		color: var(--text-secondary);
		line-height: 1.6;
		font-size: 1rem;
		margin: 0;
	}

	@media (max-width: 768px) {
		.services {
			padding: 4rem 1rem;
		}

		.section-title {
			font-size: 2.2rem;
			margin-bottom: 3rem;
		}

		.services-grid {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}

		.service-card {
			padding: 2rem;
		}
	}
</style>
