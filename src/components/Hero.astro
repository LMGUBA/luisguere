---

---

<section id="inicio" class="hero">
	<div class="hero-content">
		<div class="hero-image">
			<div class="image-frame">
				<img src="/luis12.png" alt="Luis Guere" class="profile-img" />
			</div>
		</div>
		<h1 class="hero-title">Luis Guere</h1>
		<h2 class="hero-subtitle">
			Especialista en Ingeniería de Software con Inteligencia Artificial
		</h2>
		<p class="hero-description">
			Desarrollando soluciones tecnológicas innovadoras con IA y Machine
			Learning en Huancayo, Pucallpa y todo el Perú. Explorando la
			intersección entre inteligencia artificial, desarrollo de software y
			mecatrónica.
		</p>
		<div class="hero-actions">
			<a href="#contacto" class="apple-button">Contactar</a>
		</div>
	</div>
</section>

<style>
	.hero {
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 2rem;
		padding-top: 6rem;
		background: var(--bg-color);
		position: relative;
		overflow: hidden;
	}

	.hero-content {
		text-align: center;
		max-width: 800px;
		z-index: 1;
		position: relative;
	}

	.hero-image {
		margin-bottom: 2.5rem;
		position: relative;
		display: inline-block;
	}

	.image-frame {
		position: relative;
		width: 200px;
		height: 200px;
		margin: 0 auto;
		border-radius: 50%; /* Circular profile image */
		overflow: hidden;
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.12);
	}

	.profile-img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		border-radius: 50%; /* Circular profile image */
		filter: grayscale(100%); /* Black and white aesthetic for image */
		transition:
			filter 0.5s ease,
			transform 0.5s ease;
	}

	.profile-img:hover {
		filter: grayscale(0%); /* Reveal color on hover */
		transform: scale(1.05);
	}

	.hero-title {
		font-family: var(--font-main);
		font-size: 4.5rem;
		font-weight: 700;
		margin-bottom: 1rem;
		color: var(--text-primary);
		letter-spacing: -0.03em;
		line-height: 1.1;
	}

	.hero-subtitle {
		font-family: var(--font-main);
		font-size: 1.5rem;
		font-weight: 400;
		margin-bottom: 2rem;
		color: var(--text-secondary);
		line-height: 1.4;
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
	}

	.hero-description {
		font-size: 1.1rem;
		color: var(--text-secondary);
		line-height: 1.6;
		max-width: 600px;
		margin: 0 auto 3rem auto;
	}

	.hero-actions {
		display: flex;
		gap: 1.5rem;
		justify-content: center;
		margin-top: 2rem;
	}

	.apple-button {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 1rem 2.5rem;
		font-family: var(--font-main);
		font-weight: 500;
		font-size: 1rem;
		text-decoration: none;
		border-radius: 0; /* Sharp corners */
		transition: all 0.3s ease;
		background: var(--text-primary);
		color: var(--bg-color);
		border: 1px solid var(--text-primary);
	}

	.apple-button:hover {
		background: transparent;
		color: var(--text-primary);
		transform: translateY(-2px);
	}

	@media (max-width: 768px) {
		.hero-title {
			font-size: 3rem;
		}

		.hero-subtitle {
			font-size: 1.2rem;
		}

		.image-frame {
			width: 160px;
			height: 160px;
		}
	}
</style>
